import { TileMapConfig } from "./Types";

const startScene = "hello-world";

const tileMapConfig: TileMapConfig = {
  mapPath: "tilemaps",
  mapKeys: ["test"],

  setsPath: "tilesets",
};

export { startScene, tileMapConfig };
